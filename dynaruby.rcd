#!/bin/sh

. /etc/rc.subr

daemon="/usr/local/sbin/dynaruby"
name="dynaruby"

start_cmd="${name}_start"
stop_cmd=":"

dynaruby_start()
{
    echo "Starting ${name}."
    env DYNARUBY_KEY=1234 ${daemon}
}

run_rc_command "\$1"
